{
  "name": "nblmextract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "jsdom": "^26.1.0"
  }
}